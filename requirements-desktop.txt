# requirements-desktop.txt
# Dependencias ADICIONALES para la versión de escritorio.
# Las dependencias del servidor ya están en requirements.txt.
#
# Instalar todo:
#   py -3.12 -m pip install -r requirements.txt
#   py -3.12 -m pip install -r requirements-desktop.txt

# Ventana nativa del navegador (Edge WebView2 en Windows)
pywebview>=4.4

# Empaquetado como .exe standalone
pyinstaller>=6.0

# Generación del icono (solo se necesita en la máquina de build, no en el exe)
Pillow>=10.0
